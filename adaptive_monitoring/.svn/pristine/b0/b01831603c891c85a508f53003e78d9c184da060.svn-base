'use strict';

angular.module('missioncontrolApp').controller('SatManagerCtrl', function ($scope, $http, Auth, User) {
  var _this = this;

  // Use the User $resource to fetch all users
  // TODO: change to satellites
  $scope.users = User.query();

  $scope['delete'] = function (user) {
    User.remove({ id: user._id });
    $scope.users.splice(this.$index, 1);
  };

  $http.get('/api/satellites').then(function (response) {
    console.log(response);
    _this.availableSatellites = response.data;
  });
});
//# sourceMappingURL=satmanager.controller.js.map
